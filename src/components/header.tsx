// TODO: Complete the header.
import React, { FC } from "react";

import style from "../style/header.module.scss"

const setHeader: FC = () => (
    <header>
        <div>
            <h2 className={style.h2}>tyautyau56's portfolio</h2>
        </div>
    </header>
)

export default setHeader